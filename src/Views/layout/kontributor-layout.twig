<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title ?? 'Dashboard Kontributor' }}</title>
  <link rel="stylesheet" href="/asset/css/dashboard.css">

  <style>
    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      font-family: 'Segoe UI', sans-serif;
      background-color: #f4f6f8;
      min-height: 100vh;
      display: flex;
    }

    .sidebar {
      background-color: #1f2b55;
      color: #fff;
      width: 240px;
      min-height: 100vh;
      padding: 30px 20px;
      box-shadow: 2px 0 5px rgba(0, 0, 0, 0.05);
      position: fixed;
      left: 0;
      top: 0;
    }

    .sidebar h2 {
      font-size: 1.6rem;
      margin-bottom: 40px;
      font-weight: 600;
    }

    .sidebar ul {
      list-style: none;
      padding: 0;
      margin: 0;
    }

    .sidebar ul li {
      margin-bottom: 16px;
    }

    .sidebar ul li a {
      color: #ffffff;
      text-decoration: none;
      padding: 10px 15px;
      display: block;
      border-radius: 6px;
      transition: background-color 0.2s ease-in-out;
    }

    .sidebar ul li a:hover {
      background-color: #2e3d75;
    }

    .main-content {
      margin-left: 240px; /* Same as sidebar width */
      padding: 40px 30px;
      flex: 1;
      background-color: #ffffff;
      min-height: 100vh;
    }
  </style>
</head>
<body>

  <div class="sidebar">
    <h2>Kontributor</h2>
    <ul>
      <li><a href="/kontributor">Dashboard</a></li>
      <li><a href="/kontributor/postsaya">Postingan Saya</a></li>
      <li><a href="/kontributor/post">Buat Postingan</a></li>
      <li><a href="/logout">Logout</a></li>
    </ul>
  </div>

  <div class="main-content">
    {% block content %}{% endblock %}
  </div>

</body>
</html>
