{% extends "layout/layout.twig" %}

{% block content %}
<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-10 text-center">
            <h1 class="display-5 fw-bold mb-4">📚 Dashboard Knowledge Management</h1>

            {% if message %}
                <div class="alert alert-warning shadow-sm" role="alert">
                    {{ message }}
                </div>
            {% else %}
                <p class="lead mb-2">Selamat datang di sistem manajemen pengetahuan kami.</p>
                <p class="text-muted">Platform ini membantu Anda untuk menyimpan, membagikan, dan mengelola pengetahuan secara efisien berdasarkan peran Anda.</p>
                <a href="/login" class="btn btn-primary mt-4 px-4 py-2">
                    <i class="bi bi-box-arrow-in-right me-1"></i> Login Sekarang
                </a>
            {% endif %}
        </div>
    </div>

    {% if not message %}
    <div class="row justify-content-center mt-5">
        <div class="col-md-8">
            <div class="card border-0 shadow-sm">
                <div class="card-body text-center">
                    <h5 class="card-title mb-3">🔍 Apa itu Knowledge Management?</h5>
                    <p class="card-text text-muted">
                        Knowledge Management System (KMS) adalah sistem yang memungkinkan organisasi atau tim untuk mengelola pengetahuan secara terstruktur. Anda dapat membuat postingan, berbagi dokumen, dan mencari informasi dengan mudah berdasarkan kategori atau kontributor.
                    </p>
                </div>
            </div>
        </div>
    </div>
    {% endif %}
</div>
{% endblock %}
