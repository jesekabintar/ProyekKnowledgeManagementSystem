{% extends "layout/layout.twig" %}

{% block content %}
<div class="container mt-4">
    <div class="card shadow-sm">
        <div class="card-body">
            <h3 class="card-title mb-3">
                👋 Halo, {{ session.user.nama ?? session.user.username }}!
            </h3>
            <p class="card-text">
                Anda login sebagai <strong>pengguna</strong>. Di halaman ini, Anda bisa menjelajahi informasi dan layanan yang tersedia dalam sistem Knowledge Management.
            </p>

            <div class="mt-4">
                <a href="#" class="btn btn-outline-primary btn-sm">
                    <i class="bi bi-person-circle"></i> Lihat Profil
                </a>
                <a href="#" class="btn btn-outline-secondary btn-sm">
                    <i class="bi bi-chat-left-text"></i> Kirim Feedback
                </a>
            </div>

            <div class="mt-5">
    <h4>📚 Daftar Cerita Terbaru</h4>
    {% if posts %}
        <div class="list-group">
            {% for post in posts %}
                <a href="/story/{{ post.id }}" class="list-group-item list-group-item-action">
                    <div class="d-flex w-100 justify-content-between">
                        <h5 class="mb-1">{{ post.title }}</h5>
                        <small class="text-muted">{{ post.created_at|date("d M Y") }}</small>
                    </div>
                    <p class="mb-1">{{ post.content|striptags|slice(0, 100) }}...</p>
                </a>
            {% endfor %}
        </div>
    {% else %}
        <p class="text-muted">Belum ada cerita tersedia.</p>
    {% endif %}
</div>

        </div>
    </div>
</div>
{% endblock %}
